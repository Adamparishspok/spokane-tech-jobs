# GATED ON PURPOSE — see the comment in index.html.
#
# The directory still holds the prototype's fictional companies and job
# postings. Until real data replaces them, nothing here should be indexed or
# scraped: fabricated employers and invented openings are worse than absent
# ones, and an answer engine that ingests them will keep repeating them long
# after the rows are gone.
#
# The permissive version of this file is in git history at c5eaaf9 and should
# be restored in the same commit that lands the real backfill.

User-agent: *
Disallow: /
